Fixture corpus for the `manifest` parity probe.

These package.json files are inert data: they are never installed and no code
in them is executed. The dependency names and versions are drawn from the
shipped indicator lists so the probe exercises real matching paths rather than
synthetic ones.
