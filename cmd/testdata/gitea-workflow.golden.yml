# SECURITY: protect this file, .gitea/CODEOWNERS, .supplychain/**,
# and any reviewed Gitleaks/Bun policy with independent security ownership.
name: supplychain

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "17 7 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: https://github.com/actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false
      - name: Scan repository
        uses: https://github.com/noeljackson/supplychain@0123456789abcdef0123456789abcdef01234567
        with:
          path: .
          policy: strict
